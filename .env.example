# Leave empty for same-origin (Docker nginx proxies /api/ to the API container).
# Local Vite without this file defaults to http://127.0.0.1:8000.
# VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_BASE_URL=
